# Headlight using Gatsby

built from https://github.com/gatsbyjs/gatsby-starter-theme-workspace

## Develop

1. In `./gatsby-theme-cognito-appsync` run `yarn link`
2. In `./example` run `yarn link "gatsby-theme-cognito-appsync"`
